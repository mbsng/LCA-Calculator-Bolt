export const calculateEmissions = (value: number, factor: number): number => {
  return value * factor;
};

export const calculateEnergy = (value: number, factor: number): number => {
  return value * factor;
};

export const calculateWaste = (value: number, factor: number): number => {
  return value * factor;
};